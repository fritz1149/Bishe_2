
如果数据长度是100MB，发送速率是100Mbit/s，那么发送时间=数据长度/发送速率=100×2^20×8/100×10^6=8.39s。
如果B正确收完100MB的数据后，就立即向A发送确认。
再假定A只有在收到B的确认信息后，才能继续向B发送数据。
显然，这需要等待一个往返时间RTT（这里假定确认信息很短，可忽略B发送确认的时间）。
如果RTT=2s，那么可以算出A向B发送数据的有效数据率。