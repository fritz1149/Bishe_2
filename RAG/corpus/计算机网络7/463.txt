
我们再强调一下，Chord 环并非实际的网络。
在Chord 环上相邻的结点， 在地理上很可能相距非常远。
Chord 环上的每一个结点都要维护两个指针变量，一个指向其后继结点， 而另一个指向其前任结点。
例如，在图 6-36(8)中， N10 的后继结点是 N20 (沿顺时针方向 N10 的下一个结点〉， 其前任结点是N7 (沿逆时针方向 N10 之前的一个结点〉。
如果一个新的结点川3 加入进来, 那么 N20 的前任结点就变为 N13,  因而 K12 就要从 N20 的位置移到 N13,  同时 N10 的后继结点就变为 N13。