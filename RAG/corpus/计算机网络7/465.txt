
若Chord环上的标识符有m位 (现在矶 = 5〉， 则在结点刃上的指针表可设置不超过 m 个指针，指向其后继的结点。
我们先看图 6-37 中结点 N4 的指针表。
指针表中的第 2列是从 N4 可以指向的多个后继结点。
本来每一个结点仅仅指向沿顺时针方向的下一个后继结点，但现在则指向多个后继结点 (在本例中就是N7, N10 和 N20)o 第 1列的第Z行是计算〈N4+2…_l)， 用来得出后继结点。
例如，第4行乏=4，算出 (N4+2薯_l〉=N4+ 8= 12，而 Chord 环上的结点 12 的后继结点是 N20。