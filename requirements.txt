# Python 3.12 环境下的依赖包
# 基于 preprocess.py 分析得出

# 标准库包（Python 3.12 内置，无需安装）
# os - 操作系统接口
# shutil - 高级文件操作
# pickle - 对象序列化
# random - 随机数生成
# pathlib - 路径操作（在 datasets.py 中使用）

# 第三方包（需要安装）
# 注意：代码中使用了 torch.utils.data.Dataset，需要 PyTorch
torch>=2.0.0

# Fire 库用于命令行参数管理
fire>=0.5.0

# tqdm 库用于进度条显示
tqdm>=4.64.0

# 可选：如果需要更好的性能，可以安装以下包
# numpy>=1.24.0  # 数值计算（如果后续需要）
# pandas>=2.0.0   # 数据分析（如果后续需要）

# 系统依赖（需要手动安装）
# tshark - Wireshark 命令行工具，用于处理 pcap 文件
# 安装方法：
# Ubuntu/Debian: sudo apt-get install tshark
# CentOS/RHEL: sudo yum install wireshark-cli
# macOS: brew install wireshark
